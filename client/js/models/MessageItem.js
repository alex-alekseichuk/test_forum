
define([
    'backbone'
], function (Backbone) {
    'use strict';

    return Backbone.Model.extend({
        initialize: function () {
            //this.set('content', _.result(templates.pages, this.get('name')));
        }
    });
});
